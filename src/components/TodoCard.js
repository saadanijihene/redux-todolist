import React from "react";

const TodoCard = () => {
  return (
    <div>
      <h1>action</h1> <button>Edit</button> <button>Done</button>
      <button>Delete</button>
    </div>
  );
};

export default TodoCard;
